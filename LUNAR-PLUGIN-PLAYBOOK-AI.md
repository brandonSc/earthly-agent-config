# Lunar Plugin PR Playbook — Redirect

**The canonical version of this playbook lives in `lunar-lib`:**

```
lunar-lib/.ai-implementation/LUNAR-PLUGIN-PLAYBOOK-AI.md
```

That is the version Bender and all AI agents read. **Do not maintain a separate copy here** — it will diverge and cause inconsistencies.

If you need to update the playbook, edit the lunar-lib copy and push a PR to `earthly/lunar-lib`.
